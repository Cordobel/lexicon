from django.shortcuts import HttpResponse

def index(request):
    return HttpResponse("Iae mundo. Se to no app projetos que eu acho que seja a entidade projetos que representa a tabala que quero fazer")
